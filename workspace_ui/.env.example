# Supabase Configuration (copy to .env.local)
VITE_SUPABASE_URL=https://dppqrshlgysebemqgnwq.supabase.co
VITE_SUPABASE_PUBLISHABLE_DEFAULT_KEY=sb_publishable_JJ7UBaFTR3c-4dE9qw8NPw_s49O38kf

# Optional: public site URL for resource QR codes (no trailing slash). If unset, the app uses the
# current origin (recommended for most deployments). Set this when you need printed QR labels to
# always point at production while you browse from localhost.
# VITE_PUBLIC_APP_ORIGIN=https://your-app.vercel.app
